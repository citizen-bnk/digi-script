# Backend base URL. Defaults to http://localhost:4000 if unset — override
# here (copy to .env.local, which is gitignored) when testing from a phone
# on the same network; see README "Testing on a phone".
VITE_API_URL=http://localhost:4000
